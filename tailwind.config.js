/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // or 'media' if you prefer dark mode to be based on the user's system preference
    theme: {
        extend: {
            // your customizations
        },
    },
    plugins: [],
}
